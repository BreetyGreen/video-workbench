# 全自动视频工作台进度

最后更新：2026-09-04

## 2026-09-04 发布收口

### Confirmed

- 修复 Windows 剪映发现后完整控制面回归重新执行，`262 passed`；所有前端 JavaScript 入口语法检查、Python 编译、Git 差异检查和秘密模式扫描通过。
- 已提交快照通过 `scripts/verify-fresh-clone.py --dry-run`：只使用 Git 跟踪文件解包，仓库布局、doctor、首次引导、配置页、本地模式保存和服务健康检查均通过，且没有修改真实用户目录。
- Windows 原生诊断补齐非系统盘剪映发现：受限扫描 `B:\Apps\JianyingPro` 及用户本地安装目录，不遍历整盘；本机可识别 `B:\Apps\JianyingPro\11.3.0.14362\JianyingPro.exe`。

## 风险与待确认

- 发布到 GitHub 后仍需等待 Windows/macOS GitHub Actions 的真实远端检出结果；本地通过不替代 macOS 实机剪映草稿导入与客户端打开。
- 尚未收到用户真实付费课程文件，因此当前教程理解闭环由可复现的混合教学演示验证，不能替代真实课程样本验收。
- 生产服务器、域名、HTTPS、钉钉组织授权、抖音开放平台权限以及 Apple/Windows 签名身份均属于外部资源；仓库可以提供检测、构建和部署入口，但不能伪造这些身份或授权。

## 2026-09-02 教学视频多模态分段与真实闭环

### Confirmed

- 新增独立 `TutorialSegment` 证据账本，联合 ASR、首尾/场景/均匀关键帧和 OCR，把教学视频区分为老师讲解、软件操作、成片示例、片头片尾和待识别；只有老师讲解直接生成剪辑规则，操作与示例只关联同一教程中位于其前方且 30 秒内的最近规则，不会关联未来规则。
- 课程处理 API、`learned-course-recipe.json`、`tutorial-visual-analysis.json`、`tutorial-segments.json` 和审核页均展示同一份分段证据，包含时间码、转写、OCR、视觉提示、置信度和关联规则 ID。
- 一键教学演示现在使用 8 段与画面章节严格对齐的独立旁白，真实包含讲解、CapCut 操作标记和成片预览标记，并以 `lecture + software_operation + finished_example` 为硬门禁；成片示例中的“需要、字幕、结尾”等销售话术不会泄漏为教学规则。
- 媒体分析关键帧固定包含首帧、尾帧和场景起点，再用全时长均匀采样补足，避免教学录屏场景变化较小时漏掉后半段；演示叠字使用 Windows、macOS、Linux 可定位的无衬线粗体回退链。
- 公网演示素材在入库前执行完整视频解码；能被 `ffprobe` 读到但实际损坏的文件会立即切换到明确标注的合成回退素材，不再拖到最终渲染失败。
- 课程规则缩短或放宽最终时间线后会对旁白执行第二次本地适配并重建字幕，不新增 TTS 调用。真实运行 `436aec8a-a581-455b-8d19-4cc23b22d9a0` 完成，任务 `f851d75a-817a-45b6-ba31-502bbad1b143` 记录 7 段讲解、1 段软件操作、2 段成片示例；7 条规则只来自讲解段，示例销售话术泄漏检查为 `false`。
- 该成片为 1080×1920、H.264/AAC、20.633 秒，质量状态 `pass`、0 阻断；旁白 20.742 秒，与时间线相差 0.109 秒。课程规则实际改变平均镜头时长、镜头数量和 CTA 结尾，两个最终素材均来自 Wikimedia Commons 且标记为 `commercial_authorized`。
- 最新完整回归基于上述最终代码执行，`261 passed`；独立代码审查未发现剩余 P0 阻断。

### Open / External user action

- 当前分类是可复现的本地多模态规则，不是通用视觉大模型。老师边讲边操作、画中画和快速交替示例仍可能出现低置信度或按主证据归类，需要用户提供真实课程后继续做样本验收。
- 本轮在 Windows 完成真实演示；macOS 的本地字体、剪映/CapCut 草稿目录、签名、公证和客户端打开仍需 Apple 机器或 CI 产物实机验证。

## 2026-09-01 教学视频理解到剪映真实闭环

### Confirmed

- 新增可重复的一键教学演示：独立教学视频生成、真实 ASR、时间码规则抽取、独立授权素材准备、基线/规则时间线对比、渲染、质量门禁和剪映交付均由同一运行记录串联。
- 教程规则保存原文证据、起止时间、置信度和转写 SHA-256；最终镜头保存命中的规则 ID，并生成 `course-rule-trace.json` 与 `course-comparison.json`。规则未造成至少两项可观察剪辑变化时以 `course_rules_not_applied` 失败。
- 真实演示运行 `4245416a-20ed-4c5c-afdc-324a0a2c2427` 完成；教程由火山 BigASR `volc.bigasr.auc_turbo` 转写为 8 个分段并抽取 9 条规则。规则实际改变了平均镜头时长、镜头数量和 CTA 结尾，共留下 3 条命中追踪。
- 成片任务 `05753740-7a36-41d0-b406-c8034d96b712` 为 1080×1920、H.264/AAC、20.8 秒；黑场、长静音、画布、音视频流、时长和字幕等阻断项全部通过。
- 演示素材账本锁定两条 Wikimedia Commons 视频的来源、许可与 SHA-256；教学画面没有进入最终成片。
- Windows 本机直跑现在会区分宿主草稿目录与 Docker 挂载目录。上述任务已导入 `B:\JianyingData\Drafts\JianyingPro Drafts\宠物护理前后对比-5285d6a9-05753740`，15 个文件齐全、11 条媒体引用可读取，打开请求为 `launched`，作业状态为 `delivered_to_jianying`。

### Open / External user action

- 本轮真实教程 ASR 使用了当前机器已配置的 BigASR；fresh clone 在零 Key 条件下会使用本地 Whisper，首次需要联网下载模型。零 Key路径由自动化测试覆盖，但本轮没有在一台全新离线 Mac 上重复下载模型。
- Windows 剪映已被真实拉起；最终画面、字体与当前剪映版本兼容性仍应由人在客户端看一遍。macOS 的 Finder/CapCut/剪映草稿路径、Gatekeeper、签名与公证仍需 Apple 机器或 CI 产物实机验收。
- 当前改动位于 `codex/video-workbench-closure` 工作树；在合并并推送到远端默认分支之前，外部用户 clone 公共仓库不会自动获得本轮最新闭环。

## 2026-09-01 课程自动剪辑与服务器到剪映闭环

### Confirmed

- 新增 `CourseEditJob` 持久化作业和 `POST/GET /api/course-edit-jobs`：锁定课程最新配方、复制课程视频到独立任务目录并执行现有真实渲染主链。
- 商用作业只选择 `commercial_authorized` 课程视频；没有合规视频时返回 `commercial_material_not_authorized`，不会回退到未知权利素材。
- 质量报告零阻断后自动跳过强制审核；阻断项存在时保持 `quality_blocked`，不进入设备队列。
- 新增待设备队列、设备回报接口和跨平台 `scripts/sync-jianying-device.py`。助手分块下载质量报告/草稿，复用 ZIP 安全校验与原子新建草稿逻辑，导入后回报服务器。
- 新增十分钟有效、单次使用且原子消费的设备配对码；服务器只保存 HMAC-SHA256 令牌摘要。待交付作业由首次拉取它的设备原子认领，此后只有该设备能查看、下载和回报，已完成作业不能被迟到的失败回报改回失败。Caddy 仅对配对和设备 API 免 Basic Auth，其他页面与管理接口继续受保护。
- 真实验收作业 `92931343-87da-4e97-9fb4-693fc4ebfb19` 使用模拟钉钉课程的 3 条商用授权视频，生成任务 `eda6537c-18bd-4556-a15c-526c10aba54d`：3 段/3 素材源、9 秒、1080×1920、H.264/AAC，10 项门禁通过、0 阻断。
- 同步助手已把该草稿导入 `B:\JianyingData\Drafts\JianyingPro Drafts\课程自动剪辑验收-无云-dd74f6bb-eda6537c`，服务器状态回报为 `delivered_to_jianying`，剪映启动请求状态为 `launched`。
- 第二条真实作业 `a1542067-da9c-49f9-bd6d-e5ccd91266a2` 完成一次性配对、设备令牌认证、专用队列下载、媒体校验、导入与回报；草稿位于 `B:\JianyingData\Drafts\JianyingPro Drafts\课程设备配对验收-49675c0e-4d046c8e`，启动状态为 `launched`。
- 钉钉 8.2.0.260119001 已安装在 `B:\DingDing` 并运行；主程序 Authenticode 状态为 `Valid`，新增 `scripts/doctor-dingtalk.ps1` 做只读检查。
- 新增 PyInstaller 6.22.2 锁定构建、Windows/macOS 安装脚本、登录自启和 `sync-helper-v*` GitHub Release 工作流。Windows 单文件产物已真实构建并执行完整烟测：返回码 0，识别 `B:\Apps\JianyingPro\JianyingPro.exe`，确认 `B:\JianyingData\Drafts\JianyingPro Drafts` 可写并完成服务端队列请求。
- 配置助手新增“服务器交付”区域：管理员可直接生成十分钟一次性配对码，普通用户从 Release 下载助手后只配对一次，不再需要 Codex 常驻。

### Open / External user action

- 真正从钉钉组织群自动收历史网课文件，仍需要管理员创建企业内部应用、Stream 机器人并授权文件下载；当前模拟入口只替代钉钉事件来源，后续处理全部是真实代码。
- 生产服务器、域名、HTTPS 和设备访问控制尚未由用户提供，因此本次服务器/设备闭环在本机两个独立数据目录与 HTTP loopback 上验收。
- Windows/macOS 单文件构建和开机自启已实现；正式面向普通用户公开分发仍需仓库所有者提供 Authenticode 与 Apple Developer ID/公证身份。macOS 二进制及 Gatekeeper/剪映实机仍需在 Apple 机器或 CI 产物上验收。
- 合成验收素材没有可转写人声，因此本次关闭 ASR/OCR，验证的是课程规则、授权筛选、多素材剪辑、渲染、质检和剪映交付；真实课程教程的视频转写质量仍需用户提供课程文件后验收。
- 当前生产形态仍是单工作区、单实例 SQLite 和同步作业执行，适合首个受控团队部署；在开放多租户或高并发之前，还需拆分持久任务队列、对象存储和数据库，并补充租户级作业路由。

## 2026-08-31 clone 前能力与配置收口

### Confirmed

- 新增单一机器可读能力清单 `services/control-plane/app/capability_catalog.json`，把 15 项能力分为 `local_no_key`、`optional_key` 和 `external_authorization`。
- `/api/setup/status` 返回不含秘密值的能力说明；配置助手直接展示支持内容、所需配置、未配置降级路径和数据边界。
- 新增普通用户完整指南与 Codex 操作指南；README、AGENTS 和账号本人操作清单已互相链接。
- 明确本地上传、分析、自动剪辑、字幕、预览、质检和剪映草稿不需要任何云端 Key。
- 修正配置助手“本地授权素材存在就把公共素材 API 显示为已连接”的混淆；Pexels/Pixabay 只有真实外部接口配置后才显示已连接。
- 首次访问改为非强制引导：工作台直接可用，用户可选择零配置本地创作或进入配置助手；跳过后仍可从侧栏重新配置。

### Open / External user action

- 公开仓库不会包含维护者的云端或平台凭据；用户只为自己选择的增强能力配置秘密。
- 抖音搜索/发布仍取决于应用审批、接口权限和发布账号 OAuth；钉钉取决于组织应用授权；远程部署取决于服务器、SSH、域名和 HTTPS。
- macOS 本地基线保持不读取仓库 `.env`；可选凭据由用户在启动进程环境中提供。Windows/Docker 使用被 Git 忽略的根目录 `.env`。

## Outcome

当前本地主链已经能够从用户授权视频或本地授权素材库创建任务，完成分析、9:16 自动剪辑、语义字幕、旁白/原声路由、质量门禁、审核页和剪映本地草稿。2026-08-21 收口阶段不再扩散新的编辑内核，重点补齐任务卫生、合规视频来源、热点证据聚合、真实抖音交付语义和服务器部署包。

## Confirmed

- 控制台、SQLite、产物存储、Dify、豆包 ASR/TTS 和本地授权素材目录可用。
- 2026-08-21 每日自动流程创建任务 `3c4bf267-77ad-4dac-9a85-5ee8a25ddbd3`，生成 1080×1920、18.433 秒成片，8 段时间线，旁白覆盖 92.3%，质量门禁通过。
- 用户上传视频已由多个真实任务和自动化测试验证；现有授权素材目录共 11 条视频资产。
- 云端用量页能显示官方余额/赠送权益和本地调用账本；官方没有返回的字段不会伪造为剩余量。
- 测试调度器现在可以独立关闭；任务支持可恢复归档，默认队列隐藏归档记录。

## Open

- 抖音开放平台热点搜索仍缺少平台凭据；公开网页元数据搜索已接入并实测返回 3 条抖音宠物视频链接，只保存标题、链接与摘要，不下载平台视频。
- Pexels、Pixabay 和 Seedance 适配器均已实现并显示显式诊断；当前未配置各自 Key/模型端点，因此自动素材仍使用 11 条本地授权视频。
- 抖音官方交付需要应用权限和用户 OAuth；官方接口不存在“写入客户端草稿箱”，只能区分剪映本地草稿、抖音仅自己可见和抖音已发布。
- 服务器尚未指定，因此只能完成生产部署包、健康检查、备份恢复和上线手册，不能声称已线上部署。

## Next Action

1. 用户指定服务器、域名和 SSH 目标后执行生产部署。
2. 用户完成抖音应用能力审批与账号 OAuth 后启用官方发布。
3. 用户按需申请 Pexels/Pixabay/Seedance 凭据；未配置时继续使用授权上传和本地素材库。
4. 在剪映中打开已导入草稿，完成最终人工视觉终审。

## 2026-08-21 收口实施记录

- 新增独立调度器运行开关；测试环境不会再启动后台每日任务，修复了测试产物目录竞争。
- 14 条标题明确包含“验证/验收/端到端”的历史任务已设置 `historical_validation` 可恢复归档；0 条任务或产物被删除。
- 授权素材增加 `rights_status`、商品 ID、允许平台和授权有效期；待授权或已过期视频不会进入自动剪辑。
- 素材中心增加“上传授权视频”，按 SHA-256 去重并保存授权依据；Pixabay 官方 API 已作为 Pexels 后的第二视频素材源。
- 新增公开热点元数据聚合：只保存抖音/小红书结果的标题、链接和摘要，不下载平台视频；失败时回退已审核热点库。
- Seedance 视频生成客户端及配置诊断已加入，只有 API Key 与模型端点同时存在才可调用，当前不会自动产生付费请求。
- 交付状态固定为 `jianying_draft`、`douyin_self_visible`、`douyin_published`；抖音官方适配器按 `video.create.bind` 的上传/创建接口实现。
- 今日真实任务剪映草稿已导入 `B:\JianyingData\Drafts\JianyingPro Drafts\宠物治愈瞬间-290fadb1`，16 条路径重写且媒体路径校验通过；仍需在剪映中人工打开终审画面效果。
- 生产部署包已包含 Caddy TLS/Basic Auth、健康检查、持久化卷、日志轮转、备份与 SSH 部署脚本；线上部署等待用户指定服务器和域名。

## 已验证

- ArcReel `0.26.0` 使用固定容器镜像运行，健康检查与首页均返回 HTTP 200。
- FastAPI 控制面使用 SQLite 持久化任务、来源、素材哈希、教程要求、趋势记录、自动化运行和审核事件。
- 视频生产控制台 `http://127.0.0.1:8130` 可完成上传、教程与要求填写、每日定时、立即运行、任务处理、热点查看和审核跳转。
- 每日调度支持 IANA 时区、关键词、当日防重复执行与人工立即运行；运行结果保留新增趋势、处理数、失败数和警告。
- 抖音热点只使用开放平台官方 Client Token 与 `aweme.dy.video_search` 视频搜索能力；未获批前可人工导入带来源公开记录，不使用 Cookie 抓取。
- Dify 两份版本化 DSL 分别负责教程拆解与趋势分析；控制面校验结构化返回后，将目标时长和首屏钩子应用到预览及剪映草稿，并把三套文案写入审核页。
- Dify 未配置或返回无效时明确警告并回退本地基线，不把未经校验的内容写入草稿。
- FFmpeg 9.0 可生成 1080×1920 H.264/AAC 预览，并记录探测、黑帧和静音检查结果。
- 生产档已实现豆包录音文件识别极速版、本地 Whisper `large-v3`、`small` 预览模型的显式路由；逐任务云端同意、提供方、模型名与降级原因都可审计。
- 参考视频作为独立输入保存，只提取钩子、镜头长度、剪切密度、字幕/OCR 和节奏结构；时间线使用自有素材生成，不复用参考画面。
- 渲染后质量门禁检查必需交付物、音视频可播放性、画布、时长、时间线连续性、首帧钩子、黑帧、长静音和字幕；阻断失败时审核 API 拒绝批准。
- `pyJianYingDraft 0.3.0` 可生成带视频与文字轨的可编辑剪映草稿目录及 ZIP；素材路径被限制在任务目录内。
- 本机剪映导入脚本已在隔离草稿根目录验证：能展开有效草稿，第二次导入同名目录会停止且不覆盖文件。
- 钉钉 Stream 连接器使用官方 `dingtalk-stream 0.24.3`，支持官方文件下载、MIME/大小双重检查、SQLite 消息去重和任务来源追踪。
- 审核页展示预览、剪映包、证据、警告、AIGC 声明和三套文案；版权未确认、交付物缺失或清单无效时不能批准。
- 统一 Compose 可一键启动 ArcReel 与控制面，重启后旧任务、草稿、趋势和定时配置保持不变。
- 控制台已增加火山引擎官方余额/信控、有效资源包、代金券、方舟免费额度/推理活动，以及本地 ASR/TTS/Dify 逐调用账本；缺失的官方字段显示“未知”而不是伪造为 0，各官方来源独立缓存和降级；AK/SK 加密保存且所有读取接口只返回掩码。
- 默认自动剪辑节奏收紧到单镜头不超过 3 秒；低人声素材生成四段式旁白并把字幕覆盖到整段旁白，旁白型成片默认控制在 22 秒内。
- 最终回归实测：控制面 148 项测试、钉钉连接器 7 项测试全部通过；6 个前端 JavaScript 入口通过 Node 语法检查，Python 编译、Git 差异检查与生产 Compose 展开验证通过。

## 2026-08-20 产品化与成片质量验收

- 全站改为统一侧栏、移动端导航和 Iris 中性色/紫色品牌体系；工作台、审核页、用量中心、音色中心与热点雷达可互相返回，不再是孤立页面。
- 创作入口压缩为“主题 + 素材”两项必填；内容类型、官方音色、质量档位、参考片与云端授权集中到可折叠高级设置。
- 云端用量按“官方余额 / 官方赠送资源 / 本地预算 / 本机调用账本”分层，并增加每条视频的 ASR、TTS、Dify 与调用事件明细。
- 音色中心接入火山引擎官方 Speaker ID 预设与真实试听；Vivi 2.0 已用当前账号验证，未获当前账号授权的音色不会伪装为可用。
- 热点雷达支持抖音开放平台官方搜索入库；小红书只导入人工核验的公开链接和证据，不绕过登录抓取。
- 商品介绍/教程任务默认生成语义分段字幕、关键词强调与全程旁白；旁白长于时间线时只在本地做限速适配，重试复用缓存，避免重复消耗云端额度。
- 真实宠物任务 `70a40128-8cc9-4e66-b5b1-90229dac4251` 已完成：1080×1920、18.482 秒、H.264/AAC、8 段字幕、旁白覆盖 98.3%、最长静音 0 秒，全部阻断质量门禁通过。
- 该任务的旁白原始 19.512 秒，本地以 1.0773× 适配为 18.168 秒；最终重试命中本地缓存，没有再次调用云端 TTS。
- Chromium 已在 1440×1000 和 390×844 验收工作台、审核页、音色、热点和用量页面；修复了音色/热点桌面宽度重复计算与工作台手机动作区溢出，DOM 实测无全页横向溢出。
- 剪映检测现已覆盖 `B:\Apps\JianyingPro`；验收草稿已安全导入 `B:\JianyingData\Drafts\JianyingPro Drafts`，未覆盖既有工程。首次在剪映中打开后的兼容性确认仍是人工步骤。

## 2026-08-20 自动素材与每日闭环验收

- 新增授权素材中心 `/materials`：展示素材来源、版权依据、使用次数与可播放预览；最终以 1440×1000、390×844 实测素材中心、创作页和最新审核页共 6 个视图，均无横向溢出。
- 新增 Pexels 官方 Video Search 适配器；有 Key 时请求竖屏 MP4 并保存作者、原页和 License，无 Key 或调用失败时回退本地授权目录。
- 已把 54 条任务素材中的版权确认视频同步为 11 条 SHA-256 去重授权资产；本地目录不会收录未确认版权的文件。
- 每日流程现会自动同步/获取素材、按关键词和日期防重建任务、处理新任务，并在运行记录中返回素材状态、素材数和新任务审核链接。
- 中文主题匹配支持“萌宠/毛孩子→宠物”等常见同义词；首个关键词当天已完成时，会继续尝试下一个关键词，而不是提前停止。
- 真实运行 `30da660e-0f79-4f28-989d-4aea535dcee5` 从本地授权目录取 3 条素材，创建并完成任务 `8ad6abd2-c324-4350-ab48-832b01113854`，18.433 秒、旁白覆盖 92.5%、12 项门禁通过。
- 第二次真实运行 `6ebd3594-12fa-4201-b3fd-ee741418c4f7` 自动跳过当日“宠物”，转入“萌宠”并创建任务 `855280e1-f3e9-4b11-ab96-e3b636d34c43`。首屏标题已缩短为“萌宠治愈瞬间”；补齐结尾行动句后重新生成，17.433 秒、旁白覆盖 90.9%、全部阻断门禁通过。
- 两条自动任务均生成完整 MP4、ASS/SRT、封面、审核清单和剪映草稿。最终草稿 `萌宠治愈瞬间-73f2f98d` 已导入 B 盘；导入器会把容器 `/data/...` 路径重写为草稿自身 `assets` 绝对路径，本次 14 条引用全部重写且 0 条媒体缺失。
- 抖音凭据仍未配置，因此两次运行均如实记录 `douyin_search_not_configured`；没有伪造抖音或小红书热点结果。

## 本次现场验收

- 控制台首页 HTTP 200，能读取已有任务、每日配置与三个外部连接状态。
- 验收任务 `33e41d9b-0439-4f4b-9c1b-fd487d929614` 已完成 2 条语音素材与独立参考视频的真实上传、转写、参考节奏引导、FFmpeg 转码、字幕、封面、剪映草稿和审核包，状态为 `reviewing`。
- 成片时长 9.8 秒、画布 1080×1920，10 项质量检查中 9 项通过、0 项阻断；`transcription_grade` 因验收采用 `fast_preview` 而警告，符合预期。
- 审核地址：`http://127.0.0.1:8130/review/33e41d9b-0439-4f4b-9c1b-fd487d929614`。
- Chromium 在 1440×1000 与 390×844 两档完成页面验收：首页、制作档位、云端同意、参考上传、质量门禁和审核操作均可见，无控制台错误或页面横向溢出。
- 手工自动化运行 `e5b12200-f730-42a5-8ac2-89ce4ed73d9e` 已落库；因抖音凭据未配置，状态为 `completed_with_warnings`，无任务失败。

## 尚未配置的外部授权

- 钉钉：创建 Stream 机器人并填写 Client ID、Client Secret；配置后用 `start.ps1 -EnableDingTalk` 启动连接器。
- 抖音开放平台：填写 Client Key、Client Secret、Device ID，并为应用申请 `aweme.dy.video_search` 能力。
- Pexels：官方搜索适配器已就绪，但当前未配置 API Key；每日任务已验证会安全回退到 11 条本地授权素材。
- 本机剪映：已检测 B 盘安装并导入草稿；首次在当前剪映版本中打开该草稿仍需人工确认兼容性。

## 合规边界

- 当前交付到“人工审核通过的待发布包”，不使用浏览器 Cookie 做无人值守抖音发布。
- 公开点赞数等只能作为公开趋势证据；完播率、转粉率、成交等必须来自账号所有者授权数据。
- 外部凭据缺失不会被伪装成已连接，界面和 API 均显示 `not_configured`。
